apiVersion: v1
kind: Service
metadata:
  name: grafana-service
  namespace: aroundu
  labels:
    app.kubernetes.io/name: grafana
    app.kubernetes.io/part-of: aroundu
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: grafana
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP

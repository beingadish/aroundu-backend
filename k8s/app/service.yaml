apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: aroundu
  labels:
    app.kubernetes.io/name: aroundu-app
    app.kubernetes.io/part-of: aroundu
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: aroundu-app
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
